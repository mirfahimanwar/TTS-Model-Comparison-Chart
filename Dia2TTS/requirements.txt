# Dia2TTS requirements
#
# setup.ps1 handles all installation automatically.
# This file documents dependencies for reference only.
#
# ── Core (installed via pip install -e dia2/) ──────────────────────────────
# torch>=2.8.0              (installed separately from cu128 index -- see setup.ps1)
# torchaudio
# numpy>=2.1.0,<3.0
# transformers>=4.55.3
# safetensors==0.5.3
# huggingface-hub>=0.24.7
# sphn>=0.2.0               (Kyutai Mimi audio codec)
# soundfile>=0.12.1
# whisper-timestamped>=1.14.2  (auto-transcribes voice cloning reference audio)
# gradio>=4.44.1
#
# ── Extras ────────────────────────────────────────────────────────────────
# sounddevice   # for --play support (pip install sounddevice)
#
# ── PyTorch install ────────────────────────────────────────────────────────
# Requires CUDA 12.8 drivers. Install with:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128
